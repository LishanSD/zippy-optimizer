// measure_index.cpp — MeasureIndex stub (Extension B, Phase 6).
// Implementation is header-only (measure_index.h). This file exists so the
// build command that lists measure_index.cpp compiles without error.
#include "measure_index.h"
